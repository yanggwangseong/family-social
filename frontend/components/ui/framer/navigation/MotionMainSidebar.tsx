import React, { FC } from 'react';
import { motion } from 'framer-motion';

const variants = {
	open: { opacity: 1, x: 0 },
	closed: { opacity: 0, x: '-100%' },
};

const MotionMainSidebar: FC = () => {
	return <div></div>;
};

export default MotionMainSidebar;
